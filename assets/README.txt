Put your real project logo here as "logo.png" (referenced by js/config.js).
Recommended: ~300x120px, transparent background, PNG or SVG.
If this file isn't here, the booking sheet just quietly hides the broken image instead of showing an error.
